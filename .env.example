# URL publique de l'API backend (sans slash final).
# En local : http://localhost:5000
# En production (variable à définir dans Vercel > Settings > Environment Variables) :
REACT_APP_API_URL=http://localhost:5000
